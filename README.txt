Build with build.sh or manually with:

g++ -std=c++11 PngChunkParserTest.cpp PngChunkParser.cpp -o PngChunkParserTest

Test with:

./PngChunkParserTest

You will need an image with the appropriate metadata renamed to test.png in the same directory.
